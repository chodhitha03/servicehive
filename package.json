{
  "name": "servicehive",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "build": "npm --prefix frontend run build && npm --prefix backend run build"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
